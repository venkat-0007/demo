pipeline { 
    agent any
    stages {
        stage('Build') { 
            steps { 
                sh 'jenkins --version'
                echo "jenkins version display stage" 
            }
        }
    }
}